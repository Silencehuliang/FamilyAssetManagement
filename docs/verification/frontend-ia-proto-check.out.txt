=== 1. 语法与运行期：通过（脚本已完整执行） ===

=== 2. 步数度量（由原型内 BFS 算出） ===
口径：一次点击 = 1 步（不含键盘输入）；「保存」本身计 1 步
  A 五 Tab 平铺：首页→记账→保存 2 步 | 记完→看流水 1 步 | 首页→股票 1 步 | 最坏(从 expense)→保存 3 步
  B 四 Tab + 分段：首页→记账→保存 2 步 | 记完→看流水 1 步 | 首页→股票 2 步 | 最坏(从 expense)→保存 3 步
  C 三 Tab 首页聚合：首页→记账→保存 2 步 | 记完→看流水 2 步 | 首页→股票 1 步 | 最坏(从 ledger)→保存 3 步
  D 抽屉菜单：首页→记账→保存 3 步 | 记完→看流水 2 步 | 首页→股票 2 步 | 最坏(从 home)→保存 3 步

=== 3. 每个变体 × 每个页面都渲染一次（暴露模板异常） ===
  渲染成功 52 组；失败 0 组

=== 4. 上游派生要求是否在渲染结果里可见（关键字扫描） ===
  ✓  全局「待同步」指示器（#10 R1）  ← 关键字 "待同步"
  ✓  流水项待同步视觉状态（#10 R2）  ← 关键字 "b-pend"
  ✓  登出前「N 条待同步」确认（#10 R3）  ← 关键字 "待同步记录会保留至下次登录"
  ✓  iOS 安装引导条（#10 R4）  ← 关键字 "添加到主屏幕"
  ✓  离线置灰且说明原因（#10 R5）  ← 关键字 "离线不可用"
  ✓  没有注册页（#10 R6 / #9）  ← 关键字 "无「注册」入口"
  ✓  不给访客留位置（#10 R6）  ← 关键字 "无访客链接"
  ✓  SW 更新提示文案（#10 R7）  ← 关键字 "不会丢失待同步记录"
  ✓  离线行情双时间戳（#10 R8）  ← 关键字 "缓存于"
  ✓  首页汇总「另有 N 条待同步未计入」（#10 R9）  ← 关键字 "未计入汇总"
  ✓  standalone 硬门禁提示（#10 Q5 / 门禁 1）  ← 关键字 "manifest.display"
  ✓  移动端金额键盘（#10 实现约定）  ← 关键字 "inputmode=\"decimal\""
  ✓  金额输入框确实是 text + decimal，不是 number  ← 关键字 "type=\"text\" inputmode=\"decimal\""
  ✓  虚拟行「未细分」（#4）  ← 关键字 "未细分"
  ✓  「未指定对象」桶（#4）  ← 关键字 "未指定对象"
  ✓  上期无数据不给变化率（#4）  ← 关键字 "无上期数据"
  ✓  毫秒/秒混单位的坑（#14→#11）  ← 关键字 "scheduled_at"
  ✓  除权两类成分（#13 R11）  ← 关键字 "采纳只解决送转那一半"
  ✓  缺口提示条次序（#13）  ← 关键字 "补数在前"
  ✓  发生时间不可指向未来（#6）  ← 关键字 "不可指向未来"