# 家庭财务管理系统

家庭自用的财务记录系统，含两个子系统：以标签为核心的**支出子系统**，与记录持仓的**股票子系统**。

本文件是本项目领域的**词汇表**，只定义术语，不含设计与实现。

## 子系统

**支出子系统 (Expense System)**：
记录家庭支出，并以标签为核心组织、汇报与分析。
_Avoid_: 记账系统、账本、消费管理

**股票子系统 (Portfolio System)**：
成员记录各自持仓与未交易金额，用于查看每日账户收益。不管理买入卖出。
_Avoid_: 投资系统、交易系统、持仓管理

## 主体

**成员 (Member)**：
本系统的记账与持仓主体，**同时就是系统的登录身份**；两者是同一个概念，不独立存在。本系统恰有两位成员。
_Avoid_: 用户、User、账号、账户

**家庭成员**：
一个**非成员**概念。孩子、老人等不登录系统的家庭角色不是成员；涉及这类对象的支出一律以标签表达。
_Avoid_: 用「家庭成员」指代成员

## 记账

**Entry（支出记录）**：
一次消费的完整记录。本系统**只记支出，不记收入**。每一笔 Entry 归属且仅归属一位成员。
_Avoid_: Transaction、交易、流水、账单、凭证

**归属 (Attribution)**：
一笔 Entry 算在哪位成员名下。
本系统**不支持代记**：Entry 的录入者恒等于它的归属成员，二者不做区分。
_Avoid_: 归属人、记账人、owner

## 标签

**标签 (Tag)**：
描述一笔支出「花在哪、为谁花」的自由维度，**完全替代传统分类**，由成员自行创建与维护。标签分**类别标签**与**对象标签**两类，角色不同，但同属「标签」这一概念。
_Avoid_: 分类、类目、科目、category

**类别标签 (Category Tag)**：
回答「这笔钱**花在什么上**」。构成一棵**两级树**（组 › 标签），其中**组本身也可以被直接打在一笔支出上**。一笔 Entry **有且仅有 1 个类别标签**。
_Avoid_: 主标签、一级分类、二级分类

**对象标签 (Object Tag)**：
回答「这笔钱**为谁花**」。不构成层级，是一组平铺的标签；一笔 Entry 可打 **0–4 个**。孩子、老人等**非成员**的家庭角色，只以对象标签表达。
_Avoid_: 家庭成员标签、受益人

**标签树 (Tag Tree)**：
类别标签的层级结构，**最多两级**。系统只维护它的一份当前状态。
_Avoid_: 标签体系、分类树

**标签基线 (Tag Baseline)**：
标签树在当前时刻的唯一权威状态。**历史记录不保存「当时所打标签」的快照**，一律按当前基线解释。
_Avoid_: 快照、历史版本

**默认标签 (Built-in Tag)**：
系统预置的标签，**完全不可变**——不可删除、改名、移动、归档。默认集是**通用最小集**；只能新增**自建标签**与之并存。
_Avoid_: 系统标签、内置分类

**自建标签 (Custom Tag)**：
成员自行创建的标签，可新增、改名、移动、归档、删除。**删除仅限「零历史引用」**；已被历史引用的标签只能**归档**（从录入选项隐藏，历史完整保留，可恢复）。
标签**合并**指把一个标签并入另一个标签；合并**不改写任何历史记录**，只记录合并去向，查询时归并显示。
_Avoid_: 用户标签、自定义分类

## 尚未定盘

以下词汇尚未定盘，本文件暂不收录，其定义随寻路地图上对应的决策票落定：

- 预算
- 汇报周期与时间口径
- 成本价、持仓数量、未交易金额、每日收益
