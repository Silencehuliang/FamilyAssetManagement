{
  "name": "hujia-ledger-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Pages 项目根 —— 静态前端（React + Vite）+ Functions API。",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.functions.json && tsc --noEmit -p tsconfig.node.json",
    "build": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.functions.json && tsc --noEmit -p tsconfig.node.json && vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev",
    "pages:deploy": "wrangler pages deploy"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-router-dom": "^7.18.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260917.1",
    "@types/node": "^22.20.3",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^7.0.2",
    "vite": "^6.4.3",
    "wrangler": "^4.133.0"
  }
}
