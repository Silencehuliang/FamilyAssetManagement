# ============================================================================
# Pages 静态层响应头（源文件在 public/，构建时原样复制到 dist/ 根）
# 契约：docs/specs/deploy-topology.md §6.2 ③
#
# 🔴 两条纪律：
#   1. **不得**在这里写 `/api/*` 的响应头 —— 官方逐字：
#      "Custom headers defined in the `_headers` file are not applied to responses
#       generated by Pages Functions, even if the request URL matches a rule."
#      `/api/*` 的头必须在 Function 代码里往 Response 上加（§11.11）。
#   2. 限制：最多 100 条规则、每行 ≤ 2000 字符。
#      `_headers` 文件本身不会被当作静态资源提供。
# ============================================================================

# SW 必须每次回源校验 —— 否则新版本 SW 永远拿不到，更新流程失效。
/sw.js
  Cache-Control: no-cache

# manifest 同理：改 display / icons 后要能立刻生效（display 是离线能力硬门禁）。
/manifest.webmanifest
  Cache-Control: no-cache

# Vite 产物文件名带内容哈希 ⇒ 可以长期 immutable。
/assets/*
  Cache-Control: public, max-age=31536000, immutable
